terraform {
  backend "s3" {
    bucket         = ".... I did not run the cloudformation for the creation of the bucket and the dynamodb
    key            = "dev/terraform.tfstate"
    region         = "eu-west-3"
    dynamodb_table = "prueba-it-terraform-lock"
    encrypt        = true
  }
}
